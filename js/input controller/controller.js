'use strict'

class Controller {

  constructor(){ };
  
  bindActions(action){ };

  addActionProperties(oldAction, newAction){ };

  attach( target, scope ){ };

  detach( target ){ };

  removeInput( input ){ };
}