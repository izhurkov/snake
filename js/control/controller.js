'use strict'

class Controller {

  constructor(){ };

  init(){ };

  bindActions(action){ };

  addActionProperties(oldAction, newAction){ };

  attach( target, scope ){ };

  detach( target ){ };

  removeInput( input ){ };
}